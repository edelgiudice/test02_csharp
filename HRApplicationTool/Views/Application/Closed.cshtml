@model HRApplicationTool.Models.ToolConfiguration

@{
    ViewBag.Title = "Registration Closed";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="jumbotron">
    <h1>Sorry !</h1>
    <p class="lead">The registration can be submitted only between @Model.StartHour.ToString("00"):@Model.StartMin.ToString("00") and @Model.EndHour.ToString("00"):@Model.EndMin.ToString("00")</p>
     @Html.RouteLink("Return Home", new { action = "Index", controller = "Home" },new { @class="btn btn-primary btn-large" })
</div>